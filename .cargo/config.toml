# Cross-compilation linker for Windows GNU target.
# Used when building inside Docker (Linux -> Windows cross-compile)
# and when building natively with the MSYS2/MinGW toolchain on Windows.
[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
